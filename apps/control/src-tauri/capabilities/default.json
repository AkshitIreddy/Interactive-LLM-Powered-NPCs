{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "response-console",
  "description": "Minimal capability surface for the Response Console WebView. Product operations are exposed only as typed application commands.",
  "windows": ["main"],
  "permissions": []
}
