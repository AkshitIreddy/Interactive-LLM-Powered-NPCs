[package]
name = "npc-provider-catalog"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Validated provider, model, privacy, and voice-intent catalog for Interactive LLM Powered NPCs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10"
thiserror = "2.0"
