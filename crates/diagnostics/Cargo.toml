[package]
name = "interactive-npcs-diagnostics"
version = "2.0.0-alpha.1"
edition = "2021"
license = "MIT"
description = "Privacy-preserving local diagnostics and egress policy"

[dependencies]
regex = "1.11"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10"
thiserror = "2.0"
