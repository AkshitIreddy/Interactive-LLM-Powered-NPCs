[package]
name = "npc-provider-loadouts"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Versioned provider/model loadouts for Interactive LLM Powered NPCs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
thiserror = "2.0"

[dev-dependencies]
serde_json = "1.0"

[lints]
workspace = true
